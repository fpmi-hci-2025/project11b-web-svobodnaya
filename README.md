# TaskFlow Web Frontend

Веб-приложение для управления проектами на Vue.js 3.

## 🛠️ Технологии

- **Vue.js 3** — фреймворк (Composition API)
- **Vue Router 4** — маршрутизация
- **Pinia** — управление состоянием
- **Axios** — HTTP-клиент
- **Vite** — сборщик
- **Vitest** — тестирование
- **ESLint + Prettier** — линтинг и форматирование
- **Docker + Nginx** — контейнеризация и раздача статики

## 🚀 Запуск

### Режим разработки

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev
```

Приложение доступно на http://localhost:5173

### Production сборка

```bash
# Сборка
npm run build

# Предпросмотр
npm run preview
```

### С Docker Compose

```bash
# Из корня репозитория
cd ..
docker-compose up --build

# Или только фронтенд
docker-compose up --build frontend
```

Приложение доступно на http://localhost:8080

## 📱 Страницы

| Путь | Компонент | Описание |
|------|-----------|----------|
| `/login` | LoginView | Страница входа |
| `/register` | RegisterView | Страница регистрации |
| `/projects` | ProjectsView | Список проектов пользователя |
| `/projects/:id` | ProjectDetailView | Детальная страница проекта с задачами |

## 🎨 Функциональность

### Аутентификация
- Регистрация нового пользователя
- Вход по логину/паролю
- Автоматический редирект неавторизованных пользователей
- Хранение JWT токена

### Проекты
- Просмотр списка своих проектов
- Создание нового проекта
- Редактирование и удаление проектов
- Управление участниками (добавление/удаление)

### Задачи
- Создание задач в проекте
- Редактирование задач (заголовок, описание, статус, сложность, ответственный)
- Удаление задач
- **Два режима отображения:**
  - 📋 Список — все задачи в виде списка
  - 📊 Kanban-доска — колонки по статусам (To Do, In Progress, Review, Done)

### UI/UX
- Современный дизайн в оранжевой цветовой схеме
- Адаптивная вёрстка
- Интуитивная навигация

## 📁 Структура проекта

```
project11b-web-svobodnaya/
├── src/
│   ├── api/
│   │   └── index.js        # API клиент (Axios)
│   ├── assets/
│   │   └── style.css       # Глобальные стили
│   ├── components/
│   │   └── AppHeader.vue   # Шапка приложения
│   ├── router/
│   │   └── index.js        # Маршруты Vue Router
│   ├── stores/
│   │   ├── auth.js         # Стор аутентификации
│   │   ├── projects.js     # Стор проектов
│   │   └── tasks.js        # Стор задач
│   ├── views/
│   │   ├── LoginView.vue
│   │   ├── RegisterView.vue
│   │   ├── ProjectsView.vue
│   │   └── ProjectDetailView.vue
│   ├── App.vue             # Корневой компонент
│   └── main.js             # Точка входа
├── public/
│   └── favicon.svg
├── tests/
│   └── App.spec.js
├── index.html
├── vite.config.js
├── package.json
├── Dockerfile
├── nginx.conf
└── README.md
```

## 📜 Скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера с hot-reload |
| `npm run build` | Production сборка |
| `npm run preview` | Предпросмотр production сборки |
| `npm test` | Запуск тестов |
| `npm run test:watch` | Тесты в watch-режиме |
| `npm run lint` | Проверка ESLint |
| `npm run format` | Форматирование Prettier |

## ⚙️ Конфигурация API

API endpoint настраивается в `src/api/index.js`. По умолчанию используется относительный путь `/api`, который проксируется nginx на бэкенд.

Для локальной разработки без Docker настройте `vite.config.js`:

```javascript
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
})
```

## 🔒 Аутентификация

- JWT токен сохраняется в localStorage
- Axios интерцептор автоматически добавляет токен к запросам
- При истечении токена — редирект на страницу входа
